<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.systemize">

    <application
        android:allowBackup="true"
        android:icon="@drawable/systemize_logo_white"
        android:label="@string/app_name"
        android:roundIcon="@color/aqua"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".TaskSelection" />
        <activity android:name=".NewTaskActivity" />
        <activity android:name=".OnboardingActivity1" />
        <activity android:name=".OnboardingActivity2" />
        <activity android:name=".HomeScreenActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>